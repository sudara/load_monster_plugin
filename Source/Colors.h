#pragma once

#include <juce_graphics/juce_graphics.h>

namespace Colors
{
    using namespace juce;

    const Colour pluginBackground = Colour::fromRGB (51, 62, 68);
    const Colour meterTrack = Colour::fromRGB (155, 155, 154);

    const Colour meterValue = juce::Colour::fromRGB (177, 204, 52);

    const Colour configLabelBackground = juce::Colour::fromRGB (38, 50, 56);

    const Colour brainsButton = juce::Colour::fromRGB(222,117,142);
    const Colour buttonText = juce::Colour::fromRGB(208,207,206);

    const Colour graphBackground = juce::Colour::fromRGB (38, 50, 56);
    const Colour graphValue = juce::Colour::fromRGB (177, 204, 52);
}
